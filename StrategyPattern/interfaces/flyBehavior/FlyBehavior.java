package interfaces.flyBehavior;

public interface FlyBehavior {
    public void fly();
}

// The interface only has a single method, fly(). This method is implemented by all the classes that implement this interface.

